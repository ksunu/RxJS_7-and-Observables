import { catchErrorOperator } from "./catchError";
import { debounceTimeOperator } from "./debounceTime";
import { filterOperator } from "./filter";
import { flatteningOperators } from "./flattening";
import { mapOperator } from "./map";
import { tapOperator } from "./tap";

/**
 * @filter
 */
// filterOperator()

/**
 * @map
 */
// mapOperator()

/**
 * @tap
 */
// tapOperator()

/**
 * @debounceTime
 */
// debounceTimeOperator()

/**
 * @catchError
 */
// catchErrorOperator()

/**
 * @flattening : concat, switch, merge
 */
flatteningOperators()